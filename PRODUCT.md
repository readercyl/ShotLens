# Product

## Register

product

## Users

需要在 macOS 上快速理解屏幕外语内容、并希望保留原文位置关系的用户。

## Product Purpose

框选屏幕内容后，以尽可能短的等待时间得到清晰、可靠的原位中文翻译。

## Brand Personality

克制、直接、可靠。

## Anti-references

避免抢焦点、装饰性动效、依赖颜色才能理解的状态、难以看清的控件，以及为了理论完整而拉长主流程的多次网络请求。

## Design Principles

- 结果速度优先，主流程不加入不必要的等待。
- 遵循 macOS 熟悉的控件语义和操作习惯。
- 状态变化同时通过形态表达，不能只依赖颜色。
- 浮框服务于原位译文，控制项清晰但不过度抢眼。
- 异常处理保持有界，不把一次框选放大成多次串行请求。
- 中文不翻译。OCR 动态使用 macOS Vision 支持的识别语言；每个完整语义块发送给 API，由模型翻译其中所有非中文自然语言并保留中文、数字、代码和链接。超长语义块按自然句分段请求后合并，模型漏项只定向重试，未收齐前不展示半截译文。
- 单个语义块先定向重试；仍失败时不展示半截译文，明确提示用户重新翻译，保证结果窗不会把缺行伪装成完整结果。
- OCR 坐标只用于阅读顺序、栏位判断、样式和原文字背景恢复；译文在结果窗内按完整语义流整体排版，不再被单个原始矩形裁切。背景恢复不可用时允许在原文字区域使用采样背景兜底，但不绘制扩大色块或译文卡片。

## Accessibility & Inclusion

交互图标应根据实际背景自动保持黑白对比；状态不能只靠颜色区分；不增加装饰性动效。
