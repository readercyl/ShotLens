# v1.4.2

## 更新内容

- 修复小选区只框选一个单词时，模型原样回显、恢复重试重复等待，以及译文窗口过小导致译文不可见的问题。
- 大选区翻译改用受控批次，降低兼容 OpenAI API 的上下文和输出截断风险。
- 批次格式异常时按小批次恢复，避免一次失败扇出成几十个串行请求。
- 大选区个别语义块失败时保留已完成译文并标记“部分完成”，不再让整张截图全部失败。

## 兼容性

- Apple 芯片 Mac（M1 或后续型号）。
- macOS 14.0 或更高版本。
- 继续使用用户配置的兼容 OpenAI API；不内置公共 API、Key 或模型。
