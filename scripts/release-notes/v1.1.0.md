## 更新内容

- 钉住按钮改为透明底，仅通过 pin 图标切换表示状态。
- 钉住并翻译成功后自动隐藏下侧控制区；普通状态的按钮统一为低对比中性色并补充功能提示。
- 优化 OCR 图标噪声过滤，避免把真实单字符和句首冠词误删。
- 按几何与句法连续性合并短尾行，减少尾句被强行拆段。
- 新增原位译文布局规划，防止相邻段落覆盖区域互相重叠。
- 正常截图保持批量单请求翻译；格式异常最多修复一次，不再逐条串行放大请求。
- 分别记录 OCR、布局、翻译与总耗时，持续以 3 秒内完成为优化目标。
